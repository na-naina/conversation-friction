"""Minimal setup.py for compatibility with older pip versions."""
from setuptools import setup

# All configuration is in pyproject.toml
# This file only exists for editable installs with older pip
setup()
